language: node_js
install:
  - npm install
before_script:
  - npm list
script:
  - npm run coverage
after_success:
  - './node_modules/.bin/nyc report --reporter=text-lcov | ./node_modules/.bin/coveralls'